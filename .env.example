# Mobility backend (carrolinha-BE). The AI planner is served by the same backend at /api/planner,
# and its OpenAI key lives only in the backend's environment, never in this file.
VITE_API_URL=http://localhost:8000
OPENAI_API_KEY=sample-key
OPENAI_MODEL=gpt-6-astra
